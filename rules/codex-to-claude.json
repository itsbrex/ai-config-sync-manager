{
  "source": "codex",
  "target": "claude",
  "areas": {
    "instructions": {
      "risk": "safe",
      "mapping": "AGENTS.md to CLAUDE.md"
    },
    "skills": {
      "risk": "safe",
      "mapping": "Codex skills to Claude skills"
    },
    "mcp": {
      "risk": "safe",
      "mapping": "Codex [mcp_servers.*] tables to Claude mcpServers"
    },
    "permissions": {
      "risk": "manual",
      "mapping": "Codex prefix_rule, sandbox, and MCP tool approvals to Claude permissions where reversible",
      "nativeSources": {
        "rules/default.rules prefix_rule": "Bash(<pattern>) allow/ask/deny",
        "mcp_servers.<server>.tools.<tool>.approval_mode": "mcp__server__tool allow/ask/deny",
        "sandbox_mode = workspace-write": "Write/Edit/MultiEdit review item"
      }
    },
    "hooks": {
      "risk": "manual",
      "mapping": "Codex native command hooks to Claude hooks where event/handler is reversible"
    },
    "commands": {
      "risk": "partial",
      "mapping": "Codex skills or scripts may become Claude slash commands"
    }
  }
}
