# Tech Debt Register — ai-config-sync-manager

**Generated**: 2026-06-20 (read-only assessment)
**Commit scope**: current HEAD of `main`

## Register

| ID    | Category     | Sev | Effort | Evidence                                                                                                                                                                                                                                                                                                                                                                              | Remediation                                                                                                                                                                                  |
| ----- | ------------ | --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TD-01 | complexity   | 4   | XL     | `bin/ai-config-sync.mjs` is 8,836 lines, 454 functions, 1,705 branching constructs (882 `if`, 213 `for`, 449 ternaries) in a single module with zero exports. CLAUDE.md acknowledges splitting is "on hold."                                                                                                                                                                          | Extract cohesive domains (transforms, file-IO, rule-loading, rendering, commands) into `bin/util/*.mjs` modules behind a barrel. The existing `yaml-scalar.mjs` extraction is the precedent. |
| TD-02 | coverage     | 4   | L      | No code-coverage tooling installed (no c8, nyc, istanbul). 294 tests exist but there is no way to measure which of the 454 functions are actually reached. Entire functional areas lack any test path: call transforms, codex prose transforms, reference markdown generation, agent model mapping, codex native hooks, sync state persistence, `commands` area.                      | Add `c8` as a devDependency, wire `c8 node --test` into CI, set a baseline threshold (~60%), and ratchet upward.                                                                             |
| TD-03 | size         | 3   | L      | `tests/cli-fixtures.test.mjs` is 7,061 lines — a single test file covering 242 test cases across every CLI feature. Finding and editing tests is high-friction.                                                                                                                                                                                                                       | Split into per-command or per-area test files (status, sync, paraphrase, connect, reference, etc.) mirroring the integration test structure.                                                 |
| TD-04 | complexity   | 3   | M      | 9 source functions exceed 80 lines: `createOperation` (224 ln, L1360), `runParaphrase` (204 ln, L7911), `compareSkillDirs` (90 ln, L5936), `renderSyncPlan` (88 ln, L1730), `skillDirsLineEquivalent` (88 ln, L7231), `runParaphraseRegister` (85 ln, L8115), `applyMergeAgents` (84 ln, L3862), `transformCodexProseReverse` (81 ln, L2572), `skillDirChangePreview` (81 ln, L3281). | Break each into smaller single-responsibility helpers. `createOperation` (224 ln) and `runParaphrase` (204 ln) are the priority targets.                                                     |
| TD-05 | smells       | 3   | S      | 12 bare/nameless `catch` blocks silently discard errors (L4367, L5401, L5423, L5438, L5713, L6128, L6139, L6246, L6752, L6964, L7595, L4379 in `bin/ai-config-sync.mjs`). Debugging file-read or parse failures requires adding temporary logging.                                                                                                                                    | Add a project-wide `tryCatch(fn, fallback, log?)` utility or at minimum give each `catch` block a named parameter and a debug-level log call.                                                |
| TD-06 | complexity   | 3   | M      | 605 lines in `bin/ai-config-sync.mjs` have 4+ nesting levels (8+ leading spaces); 23 lines reach 6+ levels. Deepest nesting occurs in `createOperation`, MCP normalization (L5274-5284), and `checkParaphraseMapEquivalence` (L8147-8155).                                                                                                                                            | Apply early-return / guard-clause refactoring and extract nested logic into named helpers.                                                                                                   |
| TD-07 | duplication  | 2   | M      | 8 identical `*Source()` / `*Candidates()` function pairs (16 functions total) for rule loading follow the same 3-step pattern, differing only in the JSON filename. Lines: 1122-1963, 6512-6520.                                                                                                                                                                                      | Replace with a generic factory: `makeRuleLoader(filename, defaults, mergeFn)` returning `{ source(), candidates() }`.                                                                        |
| TD-08 | duplication  | 2   | S      | `tryConnectAction` (L7529) and `tryConnectActionAsync` (L7541) are structurally identical, differing only by `await`.                                                                                                                                                                                                                                                                 | Merge into a single async function (sync callers can ignore the promise or use `tryConnectActionAsync` universally since the CLI is already async at the top).                               |
| TD-09 | smells       | 2   | S      | `error instanceof Error ? error.message : "Unknown ..."` repeated at 6 call sites (L69, L3069, L3746, L7536, L7548 in source; L48 in `scripts/sync-plugin-cache.mjs`).                                                                                                                                                                                                                | Extract a one-liner `errorMsg(e)` utility into `bin/util/`.                                                                                                                                  |
| TD-10 | duplication  | 2   | S      | `mkdirSync(dirname(path), { recursive: true })` + `writeFileSync(path, ...)` repeated at 15+ sites in `bin/ai-config-sync.mjs`.                                                                                                                                                                                                                                                       | Extract a `writeFileSafe(path, content)` helper into `bin/util/`.                                                                                                                            |
| TD-11 | smells       | 2   | S      | `scripts/dashboard.mjs` L996: SSE event parse error silently swallowed in `try{render(JSON.parse(ev.data));}catch(e){}`. A malformed SSE payload causes the dashboard to silently stop updating with no user feedback.                                                                                                                                                                | Add a minimal `console.warn` or render an error indicator in the UI.                                                                                                                         |
| TD-12 | dependencies | 2   | S      | 70 synchronous file-system calls (`readFileSync`, `writeFileSync`, `existsSync`, `readdirSync`, `mkdirSync`) in `bin/ai-config-sync.mjs`. Acceptable for a CLI but blocks the event loop in the dashboard SSE server (`scripts/dashboard.mjs`) which imports some of these paths. Flagged in the project's own `antipatterns.md`.                                                     | For the CLI, sync is fine. For the dashboard hot path, switch to async `fs/promises` equivalents or run sync work in a worker.                                                               |
| TD-13 | size         | 2   | S      | `scripts/dashboard.mjs` is 1,042 lines mixing HTTP server, SSE streaming, HTML rendering, and CLI process management.                                                                                                                                                                                                                                                                 | Extract HTML template into a separate file; split server logic from rendering.                                                                                                               |
| TD-14 | smells       | 1   | S      | 1 active TODO: `bin/ai-config-sync.mjs` L5779 — "track instructions presence/hash per host once item-level diffs land for that area."                                                                                                                                                                                                                                                 | Address when item-level diffs ship, or convert to a GitHub issue for tracking.                                                                                                               |
| TD-15 | coverage     | 1   | S      | No integration tests exist for the `claude-to-codex` direction. `tests/integration/codex-to-claude/` has 7 test files, but `tests/integration/claude-to-codex/` is absent.                                                                                                                                                                                                            | Add a parallel integration test suite for `claude-to-codex` sync to ensure bidirectional fidelity.                                                                                           |

## Severity Scale

| Score | Meaning                            |
| ----- | ---------------------------------- |
| 5     | Blocks releases                    |
| 4     | Recurring incidents / high risk    |
| 3     | Developer friction / moderate risk |
| 2     | Minor / localized issue            |
| 1     | Cosmetic / tracking only           |

## Effort Scale

| Label | Meaning   |
| ----- | --------- |
| S     | < 1 day   |
| M     | 1-3 days  |
| L     | 1-2 weeks |
| XL    | > 2 weeks |

---

## Summary

The codebase is a zero-dependency ESM CLI with 294 tests and clean dependency hygiene, but two structural risks dominate: the 8,836-line monolith (`bin/ai-config-sync.mjs`, TD-01) concentrates all logic, complexity, and duplication in a single file, and the absence of coverage tooling (TD-02) means there is no empirical signal on which of its 454 functions are actually exercised. Twelve silent `catch` blocks (TD-05) and six duplicated patterns (TD-07/08/09/10) amplify the friction. The highest-leverage first move is adding `c8` coverage (TD-02, effort S) to establish a baseline, followed by incremental extraction of utility modules from the monolith per the existing `yaml-scalar.mjs` precedent.

## Updates

- **2026-08-09 — TD-02 closed (baseline).** c8 added (`npm run coverage`, thresholds in `.c8rc.json`). Baseline: 85.54% statements, 76.15% branches, 94.45% functions on `bin/**`. Thresholds set at 84/74/92 to ratchet upward.
